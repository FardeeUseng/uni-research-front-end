import { Layout } from "antd";

export default function Footer() {
  return (
    <Layout.Footer
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      footer
    </Layout.Footer>
  );
}
